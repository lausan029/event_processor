{
  "name": "@event-processor/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types and utilities for event processor",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
